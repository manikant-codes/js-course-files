const numbers = [45, 4, 9, 16, 25];

// let sum = 0;

// for (let i = 0; i < numbers.length; i++) {
//   sum = sum + numbers[i];
// }

// console.log(sum);

// const sum = numbers.reduceRight(function (total, value, index, array) {
//   if (index <= 2) {
//     return total + value;
//   }
//   return total;
// }, 0);

// const product = numbers.reduceRight(function (total, value, index, array) {
//   return total * value;
// }, 1);

// const difference = numbers.reduceRight(function (total, value, index, array) {
//   return total - value;
// }, 0);

console.log("sum", sum);
// console.log("product", product);
// console.log("difference", difference);
