const points = [40, 100, 1, 5, 25, 10];

// let pass = true;

// for (let i = 0; i < points.length; i++) {
//   if (points[i] < 5) {
//     pass = false;
//   }
// }

// console.log("pass", pass);

// const passedTest = points.every(function (value, index, array) {
//   if (value > 0) {
//     return true;
//   } else {
//     return false;
//   }
// });

// console.log("passedTest", passedTest);
