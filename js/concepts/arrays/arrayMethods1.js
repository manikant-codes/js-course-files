// pop() method array me se last element ko remove karta hai.
// Aur jo bhi element remove hua hoo wo return karta hai.
// pop() method se array resize hota hai.
// const fruits = ["Banana", "Orange", "Apple", "Mango"];
// fruits.pop() karenge to ye output aaye ga:
// ["Banana", "Orange", "Apple"]
// Array shrink/resize hua.

// push() method array ke end me naya element add karta hai.
// Element add karne ke baad array ki nayi size/length return karta hai.
// fruits.push("Kiwi");

// shift() method array me se first element ko remove karta hai.
// Aur jo bhi element remove hua hoo wo return karta hai.
// shift() method se array resize hota hai.
// const fruits = ["Banana", "Orange", "Apple", "Mango"];
// fruits.shift() karenge to ye output aaye ga:
// ['Orange', 'Apple', 'Kiwi']
// Array shrink/resize hua.

// unshift() method array ke start me naya element add karta hai.
// Element add karne ke baad array ki nayi size/length return karta hai.
// fruits.unshift("Kiwi");

// fruits[1] = "Cherry";

// fruits.concat(vegetables, ["Oregano", "Basil", "Rosemarray"], 10, 20, 30);

// splice() method n numbers of arguments leta hai, jisme se pehla argument hota hai start position (ke kaha se start karna hai). 2nd argument ye represent karta hai ke kitne elements remove karne hai, starting from start position. Is ke baad ke arguments me aap elements de sakte ho jo start position se add karne hai.

// splice() method ka use aap array ke elements remove/replace aur add karne ke liye use karsakte hoo.

// splice() method array return karta hai un elements ke saath jo remove hue hon.

// splice() method array resize karta hai.

e = 40;
var a = [];
let b;
const c = 30;
